FROM model-comp/py-rpc:latest

RUN conda install -c conda-forge lightgbm 

WORKDIR /container

COPY impl/lgbm_container.py /container/
COPY lgbm_model_data/ /model

ENV CLIPPER_MODEL_PATH=/model/gbm_trained.sav

CMD ["python", "/container/lgbm_container.py"]